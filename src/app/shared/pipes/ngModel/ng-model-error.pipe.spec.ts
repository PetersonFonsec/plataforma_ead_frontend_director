import { NgModelErrorPipe } from './ng-model-error.pipe';

describe('NgModelErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new NgModelErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
