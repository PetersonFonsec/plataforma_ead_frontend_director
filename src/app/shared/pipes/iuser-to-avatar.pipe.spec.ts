import { IuserToAvatarPipe } from './iuser-to-avatar.pipe';

describe('IuserToAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new IuserToAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
